spring:
  application:
    name: exam_backed
  datasource:
    url: jdbc:mysql://localhost:3306/exam_final?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "123123"
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      initialization-fail-timeout: -1

mybatis:
  configuration:
    map-underscore-to-camel-case: true
server:
  port: 9889
