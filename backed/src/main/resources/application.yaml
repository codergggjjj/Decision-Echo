spring:
  application:
    name: exam_backed
  datasource:
    url: jdbc:mysql://localhost:3306/exam_final?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      initialization-fail-timeout: -1

mybatis:
  configuration:
    map-underscore-to-camel-case: true
server:
  port: 9889

advice:
  ai:
    api-key: ${ADVICE_API_KEY:sk-72e39d40388249d5bf626cf16dadc7e9}
    base-url: ${ADVICE_BASE_URL:https://dashscope.aliyuncs.com/compatible-mode/v1}
    model: ${ADVICE_MODEL:qwen-plus-latest}
