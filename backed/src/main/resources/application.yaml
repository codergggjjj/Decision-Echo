spring:
  application:
    name: exam_backed
  datasource:
    url: jdbc:mysql://localhost:3306/exam_final?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      initialization-fail-timeout: -1
      connection-timeout: 3000

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

sa-token:
  token-name: Authorization
  token-prefix: Bearer
  timeout: 86400
  active-timeout: -1
  is-concurrent: true
  is-share: true
  token-style: uuid
  is-log: false
  is-read-header: true
  is-read-body: false
  is-read-cookie: false
server:
  port: 9889

advice:
  ai:
    api-key: ${ADVICE_API_KEY:sk-72e39d40388249d5bf626cf16dadc7e9}
    base-url: ${ADVICE_BASE_URL:https://dashscope.aliyuncs.com/compatible-mode/v1}
    model: qwen-plus
